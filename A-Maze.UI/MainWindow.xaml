<Window x:Class="A_Maze.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:A_Maze"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <DataTemplate x:Key="CellTemplate">  
            <local:CellImage Width="10" Height="10" CellProperty="{Binding}"></local:CellImage>
            <!--<Button Content="{Binding}"></Button>-->
        </DataTemplate>

        <DataTemplate x:Key="MazeTemplate">
            <ItemsControl ItemsSource="{Binding}" ItemTemplate="{StaticResource CellTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </DataTemplate>

    </Window.Resources>
    <Grid Name="MainGrid">
        <StackPanel>
            <ItemsControl x:Name="MazeGrid" ItemTemplate="{DynamicResource MazeTemplate}">
            </ItemsControl>
            <Button x:Name="StartButton" Content="Button" HorizontalAlignment="Left" Width="517" Click="StartButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
